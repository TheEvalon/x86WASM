/**
 * Worker shell for future long-running machine loops.
 * Milestone 1 runs HELLO on the main thread via `run_hello_rom`;
 * this module documents the intended worker boundary.
 */
export async function createEmulatorWorker() {
  // Placeholder: dedicated worker entry arrives with interactive UI work.
  return null;
}
